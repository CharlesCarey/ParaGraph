package BoruvkasAlgorithm.Graph;

import graph.BasicUndirectedGraph;
import graph.UndirectedEdge;
import graph.Vertex;

public class MinimumSpanningTree extends BasicUndirectedGraph<Vertex, UndirectedEdge<Vertex>> {
    public MinimumSpanningTree() {
    	super("MST", "Undirected");
    }
}
